source "https://rubygems.org"

# Jekyll is the core static site generator
gem "jekyll"

# Explicitly list plugins instead of relying on github-pages
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
end
gem "kramdown-parser-gfm"
gem 'faraday-retry'
